---
import { t } from "@src/i18n/config.i18n";

// Definir tipos para los eventos
interface Event {
    id: number;
    title: string;
    organizer: string;
    date: string;
    time: string;
    location: string;
    description: string;
    icon: string;
}

// Datos de ejemplo (en una app real vendrían de una API)
const upcomingEvents: Event[] = [
    {
        id: 1,
        title: "Concierto en el Parque",
        organizer: "Municipalidad de Lima",
        date: "15-07-2025",
        time: "20:00",
        location: "Parque de la Exposición",
        description: "Concierto gratuito de música folclórica peruana con artistas locales.",
        icon: "🎤"
    },
    {
        id: 2,
        title: "Cine al Aire Libre",
        organizer: "CineClub Barranco",
        date: "16-07-2025",
        time: "21:30",
        location: "Plaza San Francisco, Barranco",
        description: "Proyección de películas clásicas bajo las estrellas. Entrada libre.",
        icon: "🎬"
    },
    {
        id: 3,
        title: "Taller de Pintura",
        organizer: "Arte & Vida Studio",
        date: "17-07-2025",
        time: "18:00",
        location: "Centro Cultural Ricardo Palma",
        description: "Aprende técnicas de acuarela para principiantes. Materiales incluidos.",
        icon: "🎨"
    }
];
---

<aside
    class="w-full lg:w-1/3 bg-white dark:bg-gray-800 rounded-lg shadow-lg dark:shadow-gray-900/20 p-4 md:p-6 lg:order-last border dark:border-gray-700"
    id="eventos"
>
    <h3
        class="text-lg md:text-xl font-bold mb-4 text-center lg:text-left text-gray-900 dark:text-gray-100"
    >
        {t("page.home.upcomingEvents")}
    </h3>
    <div class="space-y-4">
        {upcomingEvents.map((event) => (
            <div
                class="border border-gray-200 dark:border-gray-600 rounded-lg p-3 hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-colors"
            >
                <div class="flex items-start space-x-3 mb-2">
                    <span class="text-2xl">{event.icon}</span>
                    <div class="flex-1 min-w-0">
                        <h4
                            class="font-semibold text-sm md:text-base text-gray-900 dark:text-gray-100 truncate"
                        >
                            {event.title}
                        </h4>
                        <p
                            class="text-xs md:text-sm text-blue-600 dark:text-blue-400 font-medium"
                        >
                            Por {event.organizer}
                        </p>
                    </div>
                </div>
                <div class="ml-11 space-y-1">
                    <p class="text-xs md:text-sm text-gray-600 dark:text-gray-400">
                        <span class="font-medium">📅</span> {event.date} a las {event.time}
                    </p>
                    <p class="text-xs md:text-sm text-gray-600 dark:text-gray-400">
                        <span class="font-medium">📍</span> {event.location}
                    </p>
                    <p class="text-xs md:text-sm text-gray-700 dark:text-gray-300 mt-2">
                        {event.description}
                    </p>
                </div>
            </div>
        ))}
    </div>
</aside>
